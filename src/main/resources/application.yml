server:
  port: 8083

spring:
  application:
    name: paymentservice
  data:
    mongodb:
      uri: mongodb://localhost:27017/pagos
  cloud:
    aws:
      region:
        static: us-east-1
      credentials:
        access-key: test
        secret-key: test
      cloudwatch:
        endpoint: http://localhost:4566
      logging:
        log-group: practica-microservicios
        log-stream: pagos-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
